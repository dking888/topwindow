# 遮挡助手

这个程序提供了一个可自定义大小的遮挡窗口，使用 `tkinter` 构建。它非常适合用来遮挡屏幕上的特定内容，比如在观看电影时隐藏字幕。

## 使用说明

运行程序后，会出现一个可移动和可调整大小的窗口。你可以：

- **移动窗口**：通过在窗口的任意位置单击鼠标左键并拖动，移动窗口。
- **调整大小**：通过在窗口右下角的调整大小标志处单击鼠标左键并拖动，调整窗口的大小。
- **改变颜色**：通过点击窗口顶部的“黑色”或“白色”按钮，切换窗口的背景颜色。
- **关闭窗口**：点击窗口顶部的关闭按钮（✖）即可关闭窗口。